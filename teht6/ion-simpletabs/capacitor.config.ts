import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hakuli.ionsimpletabs',
  appName: 'ion-simpletabs',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
