import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.tuito.starter',
  appName: 'ion-simpletabs',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
